mutation Login($email: String!, $password: String!) {
	login(email: $email, password: $password) {
		user {
			_id
			supporting {
				_id
				name
				email
				supporters
			}
			posts {
				_id
				title
				desc
			}
		}
		errors {
			field
			message
		}
		token {
			access
			refresh
		}
	}
}
