mutation Register(
  $email: String!
  $password: String!
  $name: String!
  $bio: String
) {
  register(email: $email, password: $password, name: $name, bio: $bio) {
    errors {
      field
      message
    }
  }
}
