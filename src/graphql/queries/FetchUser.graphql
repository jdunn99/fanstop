query FetchUser($id: String!, $cursor: String) {
  fetchUser(id: $id, cursor: $cursor) {
    name
    image
    bio
    _id
    supporting {
      image
      _id
    }
    email
    supporters {
      _id
      name
    }
    posts {
      title
      _id
      createdAt
      desc
    }
  }
}
